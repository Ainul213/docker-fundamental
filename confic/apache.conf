<VirtualHost *:80>
    DocumentRoot /var/www/html
    <Directory /var/www/html>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

# Aktifkan modul rewrite untuk WordPress
LoadModule rewrite_module modules/mod_rewrite.so